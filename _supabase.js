const{createClient}=require('@supabase/supabase-js');function admin(){return createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SECRET_KEY||process.env.SUPABASE_SERVICE_ROLE_KEY,{auth:{autoRefreshToken:false,persistSession:false}})}async function userFromReq(req){const h=req.headers.authorization||'';if(!h.startsWith('Bearer '))return{error:'توکن ورود ارسال نشده است.'};const sb=admin(),{data,error}=await sb.auth.getUser(h.slice(7));if(error||!data.user)return{error:'جلسه ورود معتبر نیست.'};return{user:data.user,sb}}function isAdmin(u){return!!u?.email&&(process.env.ADMIN_EMAILS||'').split(',').map(x=>x.trim().toLowerCase()).includes(u.email.toLowerCase())}module.exports={admin,userFromReq,isAdmin};