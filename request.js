const{userFromReq}=require('../_supabase');const P={pro:{title:'حرفه‌ای',toman:199000},business:{title:'کسب‌وکار',toman:499000}};module.exports=async(req,res)=>{const a=await userFromReq(req);if(a.error)return res.status(401).json({error:a.error});const plan=req.body?.plan;if(!P[plan])return res.status(400).json({error:'پلن نامعتبر است.'});const identifier=a.user.phone||a.user.user_metadata?.phone||a.user.email;const{data,error}=await a.sb.from('payments').insert({user_id:a.user.id,plan,amount_rial:P[plan].toman*10,status:'awaiting_payment',payment_identifier:identifier,gateway:'card_transfer'}).select('id').single();if(error)return res.status(500).json({error:error.message});res.json({payment_id:data.id,plan_title:P[plan].title,amount_toman:P[plan].toman,payment_identifier:identifier})}